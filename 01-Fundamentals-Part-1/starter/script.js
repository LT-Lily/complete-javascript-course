let js = "amazing";

if (js === "amazing") alert("JS is fun!");

// let //can change later during execution of program

let age = 30;
age = 31;
// reassigning/ MUTATE the age variable.

// declaring an empty variable

//const = cannot be changed at any point in the future

const birthYear = 1991;
birthYear = 1998;

// const must have an initial value.
// always use const be default, and let only when it needs to change in the future
