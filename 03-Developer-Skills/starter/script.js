// Remember, we're gonna use strict mode in all scripts now!
"use strict";

const prettier = "test to check formatting on save.";
//single quotes to double quotes
//adds semi-colons
//removes whitespace
if (x > 0) {
  console.log(prettier);
}

const print = (word) => {
  "test";
};
//adds space to if statements
//Advantages: make code consistent

/* -- How to configure code in Prettier -- */
//create configuration file in current project folder
//see docs for default https://prettier.io/docs/en/options.html
